### Crossplane 

Crossplane is an open‑source, Kubernetes‑based framework for building and running control planes that manage infrastructure and applications using declarative APIs, without you having to write controllers from scratch.

#### Core Idea
Crossplane extends the Kubernetes control plane with Custom Resource Definitions (CRDs) and controllers so that external infrastructure (databases, clusters, networks, etc.) looks and behaves like native Kubernetes resources.